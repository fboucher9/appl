/* See LICENSE for license details */

/* Reverse include guard */
enum guard_appl_predefines_h
{
    inc_appl_predefines_h = 1
}; /* enum guard_appl_predefines_h */

/* Predefine context handle */
struct appl_context;

/* Predefine object base class */
struct appl_object;

/* Predefine chunk handle */
struct appl_chunk;

/* Predefine allocator handle */
struct appl_allocator;

/* Predefine file handle */
struct appl_file;

/* Predefine file descriptor */
struct appl_file_descriptor;

/* Predefine */
struct appl_netdevice_descriptor;

/* Predefine */
struct appl_pool;

/* Predefine */
struct appl_pool_descriptor;

/* Predefine */
struct appl_random;

/* Predefine */
struct appl_random_descriptor;

/* Predefine */
struct appl_thread;

/* Predefine */
struct appl_thread_property;

/* Predefine */
struct appl_thread_descriptor;

/* Predefine */
struct appl_mutex;

/* Predefine */
struct appl_refcount;

/* Predefine */
struct appl_binary_heap;

/* Predefine bytes handle */
struct appl_bytes;

/* Predefine bytes descriptor */
struct appl_bytes_descriptor;

/* Predefine */
struct appl_dict;

/* Predefine */
struct appl_string;

/* Predefine */
struct appl_options;

/* Predefine */
struct appl_event;

/* Predefine */
struct appl_event_descriptor;

/* Predefine */
struct appl_list;

/* Predefine */
struct appl_hash;

/* Predefine */
struct appl_hash_descriptor;

/* Predefine */
struct appl_address;

/* Predefine */
struct appl_address_property;

/* Predefine */
struct appl_address_descriptor;

/* Predefine appl_buf handle */
struct appl_buf;

/* Predefine appl_buf0 handle */
struct appl_buf0;

/* Predefine */
struct appl_download;

/* Predefine */
struct appl_download_descriptor;

/* Predefine */
struct appl_library;

/* Predefine */
struct appl_library_descriptor;

/* Predefine */
struct appl_module;

/* Predefine */
struct appl_packet;

/* Predefine */
struct appl_socket;

/* Predefine */
struct appl_socket_property;

/* Predefine */
struct appl_property;

/* Predefines */
struct appl_queue;

/* Predefines */
struct appl_queue_descriptor;

/* Predefines */
struct appl_poll;

/* Predefines */
struct appl_poll_descriptor;

/* Predefines */
struct appl_bits;

/* Predefines */
struct appl_bits_descriptor;

/* end-of-file: appl_predefines.h */
